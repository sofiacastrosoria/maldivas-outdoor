/** Radio unificado de imágenes — portadas, sliders, contenido editorial */
export const IMAGE_BORDER_RADIUS = "rounded-2xl";

/** Alias semántico para portadas de producto (PremiumProductCover) */
export const COVER_RADIUS = IMAGE_BORDER_RADIUS;
